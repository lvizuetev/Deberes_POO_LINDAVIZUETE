//*1) Programa una función que cuente el número de caracteres 
//*de una cadena de texto, pe. miFuncion("Hola Mundo") devolverá 10.

/* 
let respuesta = prompt("introduce texto  para medir su longitud");

const contarCaracteres = (cadena) => {
    try {
        
        if (typeof(cadena) !== "string") throw "ERROR: Cadena no válida, inténtalo de nuevo";
        else respuesta=cadena.length;
    }
    catch (error) {
        alert(error);
        respuesta=-1;
    }
    return respuesta;
};

let caracteres = contarCaracteres(respuesta);

if (caracteres>0) console.log (`La cadena ${respuesta} tiene una longitud de ${caracteres}`);
 */
